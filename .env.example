# Supabase (Server-side admin access for this demo)
# 無料プランや接続失敗時は「デモモード」になり、生成・プレビュー・ZIP出力は可能で保存のみ無効になります。
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_ANON_KEY"
SUPABASE_SERVICE_ROLE_KEY="YOUR_SERVICE_ROLE_KEY"

# サーバー側の秘密鍵（ユーザーのFigmaトークンを暗号化保存するために使用）
# base64(32bytes) 推奨。例: openssl rand -base64 32
D2C_ENCRYPTION_KEY="CHANGE_ME_base64_32bytes"

# For demo: set a fixed owner id (uuid string).
# You can generate one in Supabase Auth or use any valid uuid.
D2C_OWNER_ID="00000000-0000-0000-0000-000000000001"

# Figma Personal Access Token (必要時のみ)
# FIGMA_ACCESS_TOKEN="FIGMA_PERSONAL_ACCESS_TOKEN"

# Vercel など本番: Regenerate 後のリダイレクト先（例: https://your-app.vercel.app）
# 未設定時は localhost:3000 を使用します。
# NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

# 本番の安定運用（推奨）
# - Vercel では `vercel.json` の Cron が `/api/cron/generation-worker` を呼び出します（x-vercel-cron ヘッダで認証）。
# - Vercel 以外の場合は、下記のシークレットを設定し、Cron側から `Authorization: Bearer <secret>` を付けて呼び出してください。
# D2C_CRON_SECRET="CHANGE_ME_very_long_random_secret"
